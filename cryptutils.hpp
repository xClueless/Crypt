#ifndef CRYPTUTILS_HPP
#define CRYPTUTILS_HPP

#include <string>

void printByteString(const std::string& byteString);
//void printCipherText(const std::string& cipherText);

#endif // CRYPTUTILS_HPP
